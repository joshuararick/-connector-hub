# Codex Instructions

The previous blocker was that the ChatGPT project URL was not directly readable and the local folder was empty or read-only.

Use this package as the source of truth.

## Tasks

1. Import the repo into `C:\Users\joshu\OneDrive\Documents\Ai Army`.
2. Confirm the workspace is writable.
3. Run the demo command:

```bash
python -m agent_maker.cli run "Create the first autonomous workflow"
```

4. Initialize Git if needed.
5. Add real project/interview files under `source_materials/`.
6. Convert real work into eval tasks in `examples/tasks.yaml`.
7. Do not connect high-risk tools until policy gates and tests are in place.
